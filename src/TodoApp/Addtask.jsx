import React from "react";

const Addtask = () => {
  return (
    <div>
      <input type="text" placeholder="add a new  task" />
      <button>Add</button>
    </div>
  );
};

export default Addtask;
